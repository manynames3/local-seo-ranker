{
  "name": "local-seo-ranker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "node --check app.js && find functions aws \\( -name '*.js' -o -name '*.mjs' \\) -print0 | xargs -0 -n1 node --check && node --test",
    "test": "node --test",
    "dev": "wrangler pages dev .",
    "serve": "python3 -m http.server 4173"
  }
}
